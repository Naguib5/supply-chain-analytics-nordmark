# Dataset

The original database backup file (`supply_chain.bak`) is not included in this repository because of GitHub's 100 MB file size limit.

The backup file was used as the original data source for the Power BI project.